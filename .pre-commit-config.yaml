repos:
  - repo: local
    hooks:
      - id: deptry
        name: deptry
        entry: uv run --frozen --offline deptry .
        additional_dependencies: [uv]
        language: python
        always_run: true
        pass_filenames: false
